import Container from '@/components/common/parts/Container';

const Footer = (): JSX.Element => {
  return (
    <footer className="mt-20 w-full bg-theme-light py-4">
      <Container>
        {/* ナビゲーション */}
        <nav></nav>
      </Container>
    </footer>
  );
};

export default Footer;
